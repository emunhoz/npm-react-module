import { Meta, Typeset } from '@storybook/addon-docs/blocks'

import createTheme from '../../styles'

<Meta title="Introduction/Typography" />

# Typography

**Font:** Roboto

**Sizes:** and **Weights:** 400(regular), 700(bold), 900(extrabold)

**Regular:**

<Typeset
  fontSizes={[
    Number(createTheme.fontSize.s1),
    Number(createTheme.fontSize.s2),
    Number(createTheme.fontSize.s3),
    Number(createTheme.fontSize.m1),
    Number(createTheme.fontSize.m2),
    Number(createTheme.fontSize.m3),
    Number(createTheme.fontSize.l1),
    Number(createTheme.fontSize.l2),
    Number(createTheme.fontSize.l3),
  ]}
  fontWeight={createTheme.fontWeight.regular}
/>

**Bold:**

<Typeset
  fontSizes={[
    Number(createTheme.fontSize.s1),
    Number(createTheme.fontSize.s2),
    Number(createTheme.fontSize.s3),
    Number(createTheme.fontSize.m1),
    Number(createTheme.fontSize.m2),
    Number(createTheme.fontSize.m3),
    Number(createTheme.fontSize.l1),
    Number(createTheme.fontSize.l2),
    Number(createTheme.fontSize.l3),
  ]}
  fontWeight={createTheme.fontWeight.bold}
/>

**Extrabold:**

<Typeset
  fontSizes={[
    Number(createTheme.fontSize.s1),
    Number(createTheme.fontSize.s2),
    Number(createTheme.fontSize.s3),
    Number(createTheme.fontSize.m1),
    Number(createTheme.fontSize.m2),
    Number(createTheme.fontSize.m3),
    Number(createTheme.fontSize.l1),
    Number(createTheme.fontSize.l2),
    Number(createTheme.fontSize.l3),
  ]}
  fontWeight={createTheme.fontWeight.extrabold}
/>
