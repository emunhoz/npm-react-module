import * as React from 'react'
import { render } from 'test-utils'
import { {{pascalCase name}} } from '../'

describe('[Component]: {{pascalCase name}}', () => {
  it('render the component', () => {
    expect(render(<{{pascalCase name}} />)).toMatchSnapshot()
  })
})
