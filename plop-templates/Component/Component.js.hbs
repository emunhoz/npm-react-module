import * as React from 'react'
import * as S from './styled'

interface I{{pascalCase name}} {
  example?: string
}

export const {{pascalCase name}}: React.FC<I{{pascalCase name}}> = ({ example = 'example' }) => <S.{{pascalCase name}}>My new {{pascalCase name}} component {example}</S.{{pascalCase name}}>
